import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Migroskop = () => {
    return (
        <View style={styles.container}>
            <Text>Home</Text>
        </View>
    )
}

export default Migroskop

const styles = StyleSheet.create({
    container: {
        flex: 1,
    }
})